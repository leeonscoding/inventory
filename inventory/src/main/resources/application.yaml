server:
  port: 8081
spring:
  profiles:
    active: dev
  liquibase:
    enabled: false
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true